/*
========================================================================================
    Base Nextflow config file
========================================================================================
    Default resource allocations and publishDir configuration for all processes.
    Profile-specific configs (singularity.config, hpc.config) can override these.
----------------------------------------------------------------------------------------
*/

// Centralized output directory - computed once, used by all processes.
//
// The run id is derived here rather than read from params.computed_run_id: that value is
// assigned in main.nf while the workflow script runs, which is after these publishDir
// closures have captured their params, so it always resolved to the 'run_auto' fallback
// and every run overwrote the previous one's results. Keep this expression in sync with
// auto_run_id in main.nf.
def resolvedRunId = { ->
    if (params.run_id) {
        return params.run_id
    }
    def methodSuffix = params.regulator_selection_method == 'percentage' ?
        "top${params.top_n_percent_regulators}pct" : "fold${params.regulator_fold_cutoff}x"
    return "${params.top_n_genes}HVG_coherence${params.coherence_threshold}_${methodSuffix}" +
           "_clusters${params.n_clusters}_minW${params.lemontree_tight_min_weight}"
}
def baseOutputDir = { -> "${(params.output_dir ?: (params.input_dir ? params.input_dir + '/results' : './results'))}/${resolvedRunId()}" }

process {

    cpus   = { check_max( 2    * task.attempt, 'cpus'   ) }
    memory = { check_max( 4.GB * task.attempt, 'memory' ) }
    time   = { check_max( 4.h  * task.attempt, 'time'   ) }

    // Default error strategy: retry on OOM/signal kills
    errorStrategy = { task.exitStatus in [137,140,143] ? 'retry' : 'terminate' }
    maxRetries = 2

    // Bind the user's input directory and the project's PKN directory into the
    // container. This must live here rather than in singularity.runOptions: that option
    // is built while the config files are still being parsed, when --input_dir has not
    // been merged in yet, so the bind silently came out empty. containerOptions is a
    // process directive and is resolved per task, after params are final.
    containerOptions = {
        def binds = []
        if (params.input_dir) {
            binds << "--bind ${params.input_dir}:${params.input_dir}"
        }
        def pknDir = "${projectDir}/PKN"
        if (new File(pknDir).exists()) {
            binds << "--bind ${pknDir}:${pknDir}"
        }
        binds.join(' ')
    }

    // Centralized publishDir per process
    withName:LOG_PARAMETERS {
        publishDir = [ path: { baseOutputDir() }, mode: 'copy' ]
    }
    withName:PREPROCESSING_TFA {
        publishDir = [ path: { baseOutputDir() }, mode: 'copy' ]
        cpus   = { check_max( 6     * task.attempt, 'cpus'    ) }
        memory = { check_max( 24.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 3.h   * task.attempt, 'time'    ) }
    }
    withName:CLUSTERING {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree/Lemon_out" }, mode: 'copy' ]
        cpus   = { check_max( 1     * task.attempt, 'cpus'    ) }
        memory = { check_max( 2.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 6.h   * task.attempt, 'time'    ) }
    }
    withName:POST_CLUSTERING {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy', pattern: "Lemon_out/*" ]
    }
    withName:NETWORK_GENERATION {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy' ]
        cpus   = { check_max( 4     * task.attempt, 'cpus'    ) }
        memory = { check_max( 12.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 2.h   * task.attempt, 'time'    ) }
    }
    withName:SUBNETWORK_GRAPHS {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy' ]
    }
    withName:PKN_EVALUATION {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy' ]
        cpus   = { check_max( 4     * task.attempt, 'cpus'    ) }
        memory = { check_max( 12.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 2.h   * task.attempt, 'time'    ) }
    }
    withName:MODULE_VIEWER_HEATMAPS {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree/module_heatmaps" }, mode: 'copy' ]
        cpus   = { check_max( 3     * task.attempt, 'cpus'    ) }
        memory = { check_max( 8.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 1.h   * task.attempt, 'time'    ) }
    }
    withName:ENRICHMENT_ANALYSIS {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy' ]
        cpus   = { check_max( 7     * task.attempt, 'cpus'    ) }
        memory = { check_max( 28.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 4.h   * task.attempt, 'time'    ) }
    }
    withName:MODULE_OVERVIEW_INTERACTIVE {
        publishDir = [ path: { "${baseOutputDir()}/LemonTree" }, mode: 'copy' ]
        cpus   = { check_max( 4     * task.attempt, 'cpus'    ) }
        memory = { check_max( 16.GB * task.attempt, 'memory'  ) }
        time   = { check_max( 48.h  * task.attempt, 'time'    ) }
    }
    withName:SUMMARY_REPORT {
        publishDir = [ path: { baseOutputDir() }, mode: 'copy' ]
        memory = { check_max( 8.GB  * task.attempt, 'memory'  ) }
        cpus   = 2
        time   = { check_max( 1.h   * task.attempt, 'time'    ) }
    }
}

def check_max(obj, type) {
    if (type == 'memory') {
        try {
            if (obj.compareTo(params.max_memory as nextflow.util.MemoryUnit) == 1)
                return params.max_memory as nextflow.util.MemoryUnit
            else
                return obj
        } catch (all) {
            println "   ### ERROR ###   Max memory '${params.max_memory}' is not valid! Using default value: $obj"
            return obj
        }
    } else if (type == 'time') {
        try {
            if (obj.compareTo(params.max_time as nextflow.util.Duration) == 1)
                return params.max_time as nextflow.util.Duration
            else
                return obj
        } catch (all) {
            println "   ### ERROR ###   Max time '${params.max_time}' is not valid! Using default value: $obj"
            return obj
        }
    } else if (type == 'cpus') {
        try {
            return Math.min( obj, params.max_cpus as int )
        } catch (all) {
            println "   ### ERROR ###   Max cpus '${params.max_cpus}' is not valid! Using default value: $obj"
            return obj
        }
    }
}
